# Capture the full 32-second AndroidPenMotion workflow, including idle history
# response windows. Inspect trace_bounds and stats before accepting a report.
buffers { size_kb: 131072 fill_policy: RING_BUFFER }
incremental_state_config { clear_period_ms: 1000 }
duration_ms: 40000
data_sources {
  config {
    name: "linux.ftrace"
    ftrace_config {
      ftrace_events: "ftrace/print"
      atrace_categories: "gfx"
      atrace_categories: "view"
      ftrace_events: "sched/sched_switch"
      ftrace_events: "sched/sched_waking"
      atrace_apps: "art.capycanvas"
    }
  }
}
data_sources { config { name: "linux.process_stats" process_stats_config { scan_all_processes_on_start: true } } }
data_sources { config { name: "android.surfaceflinger.frametimeline" } }
