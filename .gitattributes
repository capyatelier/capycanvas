# Preserve the published crates byte-for-byte, including their line endings and
# Markdown hard breaks. The recorded unified patch also contains blank context.
vendor/wgpu/** -text -whitespace
vendor/wgpu-hal/** -text -whitespace
vendor/wgpu-types/** -text -whitespace
vendor/wgpu-color-passthrough.patch -whitespace
