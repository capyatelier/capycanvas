--- a/vendor/zrip-core/src/fse/encode.rs
+++ b/vendor/zrip-core/src/fse/encode.rs
@@ -114,7 +114,9 @@
 impl<'t> FseEncodeState<'t> {
     pub fn init(table: &'t FseEncodeTable, symbol: u8) -> Self {
         let tt = &table.symbol_tt[symbol as usize];
-        let nb_bits_out = tt.delta_nb_bits.wrapping_add(1 << 16) >> 16;
+        // Initialize with the smallest state carrying the final symbol. A full
+        // 16-bit bias can select an extra transition for one-state symbols.
+        let nb_bits_out = tt.delta_nb_bits.wrapping_add(1 << 15) >> 16;
         let value = (nb_bits_out << 16).wrapping_sub(tt.delta_nb_bits);
         let idx = (value >> nb_bits_out) as i32 + tt.delta_find_state;
         let state = table.state_table[idx as usize] as u32;
--- a/vendor/zrip-core/src/huffman/encode.rs
+++ b/vendor/zrip-core/src/huffman/encode.rs
@@ -15,6 +15,7 @@
     weights: Vec<u8>,
     max_symbol: u8,
     table_log: u8,
+    serialized_weights: Vec<u8>,
 }

 #[cfg(feature = "alloc")]
@@ -39,20 +40,19 @@
             return None;
         }

-        if max_sym as usize > 128 {
-            return None;
-        }
-
         let (weights, table_log) = compute_huffman_weights(&freqs, num_symbols)?;
         let (codes, num_bits) = build_encode_codes(&weights, table_log);

-        Some(Self {
+        let mut table = Self {
             codes,
             num_bits,
             weights,
             max_symbol: max_sym,
             table_log,
-        })
+            serialized_weights: Vec::new(),
+        };
+        table.serialized_weights = table.try_serialize_weights()?;
+        Some(table)
     }

     pub fn from_decode_table(
@@ -78,10 +78,6 @@
             }
         }

-        if max_sym as usize > 128 {
-            return None;
-        }
-
         let num_symbols = max_sym as usize + 1;
         let active_count = seen[..num_symbols].iter().filter(|&&s| s).count();
         if active_count < 2 {
@@ -97,13 +93,16 @@

         let (codes, num_bits) = build_encode_codes(&weights, table_log);

-        Some(Self {
+        let mut table = Self {
             codes,
             num_bits,
             weights,
             max_symbol: max_sym,
             table_log,
-        })
+            serialized_weights: Vec::new(),
+        };
+        table.serialized_weights = table.try_serialize_weights()?;
+        Some(table)
     }

     pub fn table_log(&self) -> u8 {
@@ -120,9 +119,16 @@
     }

     pub fn serialize_weights(&self) -> Vec<u8> {
+        self.serialized_weights.clone()
+    }
+
+    fn try_serialize_weights(&self) -> Option<Vec<u8>> {
         let explicit = &self.weights[..self.max_symbol as usize];
         let num_symbols = explicit.len();

+        if num_symbols > 128 {
+            return compress_weights(explicit);
+        }
         let mut out = Vec::with_capacity(1 + num_symbols.div_ceil(2));
         out.push((num_symbols + 127) as u8);
         let num_bytes = num_symbols.div_ceil(2);
@@ -131,7 +137,7 @@
             let lo = explicit.get(i * 2 + 1).copied().unwrap_or(0);
             out.push((hi << 4) | lo);
         }
-        out
+        Some(out)
     }

     pub fn encode_single_stream(&self, data: &[u8]) -> Vec<u8> {
@@ -285,11 +291,42 @@
         bit_lengths[sym] = depth;
     }

-    let max_bl = *bit_lengths.iter().max().unwrap();
-    if max_bl == 0 || max_bl > MAX_BITS {
+    let mut max_bl = *bit_lengths.iter().max().unwrap();
+    if max_bl == 0 {
         return None;
     }
-
+    if max_bl > MAX_BITS {
+        let mut lengths = vec![0usize; max_bl as usize + 1];
+        for &len in &bit_lengths {
+            if len > 0 {
+                lengths[len as usize] += 1;
+            }
+        }
+        for i in (MAX_BITS as usize + 1..lengths.len()).rev() {
+            while lengths[i] > 0 {
+                let j = (1..i - 1).rev().find(|&j| lengths[j] > 0)?;
+                lengths[i] -= 2;
+                lengths[i - 1] += 1;
+                lengths[j] -= 1;
+                lengths[j + 1] += 2;
+            }
+        }
+        let mut ordered = active.clone();
+        ordered.sort_unstable_by(|a, b| b.cmp(a));
+        let mut at = 0;
+        for (len, &count) in lengths
+            .iter()
+            .enumerate()
+            .take(MAX_BITS as usize + 1)
+            .skip(1)
+        {
+            for _ in 0..count {
+                bit_lengths[ordered[at].1] = len as u8;
+                at += 1;
+            }
+        }
+        max_bl = MAX_BITS;
+    }
     let table_log = max_bl;
     let mut weights = vec![0u8; num_symbols];
     for (s, &bl) in bit_lengths.iter().enumerate() {
@@ -340,6 +377,38 @@
 #[cfg(test)]
 mod tests {
     use super::*;
+
+    #[test]
+    fn full_symbol_range_and_limited_skew_preserve_both_weight_encodings() {
+        for symbols in 129..=256 {
+            for heavy in [0usize, 100_000] {
+                let mut data: Vec<_> = (0..symbols).map(|s| s as u8).cycle().take(4096).collect();
+                data.extend(core::iter::repeat_n(255, heavy));
+                let table = HuffmanEncodeTable::from_data(&data).unwrap();
+                assert!(table.table_log <= MAX_BITS);
+                let description = table.serialize_weights();
+                assert_eq!(description[0] < 128, table.max_symbol > 128);
+                let (weights, consumed) =
+                    crate::huffman::weights::parse_huffman_weights(&description).unwrap();
+                assert_eq!(consumed, description.len());
+                // The serialized description omits the final weight; the
+                // decode-table builder infers it from the remaining capacity.
+                assert_eq!(
+                    weights,
+                    table.weights[..table.max_symbol as usize],
+                    "symbols={symbols} heavy={heavy} header={description:?}"
+                );
+                let (decode, log) =
+                    crate::huffman::weights::build_huffman_decode_table(&weights).unwrap();
+                let stream = table.encode_single_stream(&data);
+                assert_eq!(
+                    crate::huffman::decode::decode_single_stream(&decode, log, &stream, data.len())
+                        .unwrap(),
+                    data
+                );
+            }
+        }
+    }

     #[test]
     fn from_decode_table_roundtrip() {
@@ -511,3 +580,54 @@
         assert_eq!(decoded, data);
     }
 }
+
+// RFC 8878 Zstandard Huffman tables with more than 128 explicit weights use
+// two interleaved FSE states. The final Huffman weight remains implicit.
+fn compress_weights(weights: &[u8]) -> Option<Vec<u8>> {
+    use crate::bitstream::writer::BitWriter;
+    use crate::fse::{
+        encode::{FseEncodeState, FseEncodeTable},
+        table_builder::{normalize_counts, serialize_fse_table_description},
+    };
+    let mut counts = [0u32; 13];
+    for &w in weights {
+        counts[w as usize] += 1;
+    }
+    if counts.iter().filter(|&&n| n > 0).count() == 1 {
+        let dummy = if counts[0] == 0 { 0 } else { 1 };
+        counts[dummy] = 1;
+    }
+    let last = counts.iter().rposition(|n| *n > 0).unwrap();
+    let dist = normalize_counts(&counts[..=last], 6)
+        .into_iter()
+        .map(|n| n.abs())
+        .collect::<Vec<_>>();
+    let table = FseEncodeTable::from_distribution(&dist, 6);
+    let mut out = serialize_fse_table_description(&dist, 6);
+    let mut writer = BitWriter::new();
+    let mut a = FseEncodeState::init(&table, weights[weights.len() - 1]);
+    let mut b = FseEncodeState::init(&table, weights[weights.len() - 2]);
+    for (i, &w) in weights[..weights.len() - 2].iter().rev().enumerate() {
+        if i % 2 == 0 {
+            a.encode_symbol(&mut writer, w);
+        } else {
+            b.encode_symbol(&mut writer, w);
+        }
+    }
+    if weights.len() % 2 == 0 {
+        a.flush(&mut writer, 6);
+        b.flush(&mut writer, 6);
+    } else {
+        b.flush(&mut writer, 6);
+        a.flush(&mut writer, 6);
+    }
+    writer.close_reverse_stream();
+    out.extend(writer.into_bytes());
+    if out.len() >= 128 {
+        return None;
+    }
+    let mut result = Vec::with_capacity(out.len() + 1);
+    result.push(out.len() as u8);
+    result.extend(out);
+    Some(result)
+}
--- a/vendor/zrip-encode/src/block_encoder.rs
+++ b/vendor/zrip-encode/src/block_encoder.rs
@@ -399,17 +399,9 @@
     workspace: &mut BlockEncodeWorkspace,
     use_custom_sequence_tables: bool,
 ) -> Result<(), CompressError> {
-    if sequences.is_empty() {
-        return encode_raw_block(src, last, output);
-    }
-
     let n = sequences.len();
-    let total_match: usize = sequences.iter().map(|s| s.match_length as usize).sum();
-    if total_match <= n * 3 {
-        return encode_raw_block(src, last, output);
-    }
-
     let saved_rep = *rep_offsets;
+    let saved_huffman = workspace.prev_huffman.clone();

     pack_sequences_and_literals(src, sequences, rep_offsets, workspace);

@@ -495,6 +487,7 @@
     let block_len = literal_section_len + seq_data.len();
     if block_len >= src.len() {
         *rep_offsets = saved_rep;
+        workspace.prev_huffman = saved_huffman;
         return encode_raw_block(src, last, output);
     }

@@ -867,6 +860,9 @@
     output.reserve(n * 4 + 4);

     write_seq_count(output, num_seq);
+    if n == 0 {
+        return;
+    }

     output.push(0x00);

@@ -958,6 +954,9 @@
     output.reserve(n * 4 + 4);

     write_seq_count(output, num_seq);
+    if n == 0 {
+        return;
+    }

     // Mode byte: repeat (3) for all three streams
     output.push((3 << 6) | (3 << 4) | (3 << 2));
@@ -1283,6 +1282,9 @@
     output.reserve(n * 4 + 64);

     write_seq_count(output, num_seq);
+    if n == 0 {
+        return true;
+    }

     output.push((ll_mode << 6) | (of_mode << 4) | (ml_mode << 2));

--- a/vendor/zrip-encode/src/dfast.rs
+++ b/vendor/zrip-encode/src/dfast.rs
@@ -298,10 +298,6 @@
         1usize << params.window_log
     };

-    let probe_interval = (block_size / 4).max(4096).min(block_size);
-    let mut probe_limit = block_start + probe_interval;
-    let mut total_match_bytes: usize = 0;
-
     let hash_log = if HASH_LOG != 0 {
         HASH_LOG
     } else {
@@ -370,7 +366,6 @@
                 if (r1 > 0) & (ip0 >= r1) && rd32!(src, ip0) == rd32!(src, ip0 - r1) {
                     let ml = count_match!(src, ip0 + 4, ip0 - r1 + 4, block_end) + 4;
                     core::mem::swap(&mut rep0, &mut rep1);
-                    total_match_bytes += ml;
                     sequences.push(Sequence {
                         literal_length: 0,
                         offset: r1 as u32,
@@ -423,7 +418,6 @@
                         }
                         let back = ip2 - ip0;
                         let mlen = count_match!(src, ip2 + 4, ip2 - r0 + 4, block_end) + 4 + back;
-                        total_match_bytes += mlen;
                         sequences.push(Sequence {
                             literal_length: (ip0 - anchor) as u32,
                             offset: r0 as u32,
@@ -457,7 +451,6 @@
                 }
                 let match_start = ip0 - back;
                 let mlen = count_match!(src, ip0 + 8, match_long + 8, block_end) + 8 + back;
-                total_match_bytes += mlen;
                 let offset = (match_start - (match_long - back)) as u32;
                 sequences.push(Sequence {
                     literal_length: (match_start - anchor) as u32,
@@ -504,7 +497,6 @@
                                 back += 1;
                             }
                             let match_start = ip0 - back;
-                            total_match_bytes += long_len + back;
                             let offset = (match_start - (ml_next - back)) as u32;
                             sequences.push(Sequence {
                                 literal_length: (match_start - anchor) as u32,
@@ -539,7 +531,6 @@
                 }
                 let match_start = ip0 - back;
                 mlen += back;
-                total_match_bytes += mlen;
                 let offset = (match_start - (match_short - back)) as u32;
                 sequences.push(Sequence {
                     literal_length: (match_start - anchor) as u32,
@@ -599,7 +590,6 @@
                 }
                 let match_start = ip0 - back;
                 let mlen = count_match!(src, ip0 + 8, match_long + 8, block_end) + 8 + back;
-                total_match_bytes += mlen;
                 let offset = (match_start - (match_long - back)) as u32;
                 sequences.push(Sequence {
                     literal_length: (match_start - anchor) as u32,
@@ -648,7 +638,6 @@
                                 back += 1;
                             }
                             let match_start = ip0 - back;
-                            total_match_bytes += long_len + back;
                             let offset = (match_start - (ml_next - back)) as u32;
                             sequences.push(Sequence {
                                 literal_length: (match_start - anchor) as u32,
@@ -685,7 +674,6 @@
                 }
                 let match_start = ip0 - back;
                 mlen += back;
-                total_match_bytes += mlen;
                 let offset = (match_start - (match_short - back)) as u32;
                 sequences.push(Sequence {
                     literal_length: (match_start - anchor) as u32,
@@ -723,15 +711,6 @@
             {
                 primitives::prefetch_ht(hash_short, hs1);
                 primitives::prefetch_ht(hash_long, hl1);
-            }
-
-            if ip0 >= probe_limit {
-                let scanned = ip0 - block_start;
-                if total_match_bytes * 6 < scanned {
-                    sequences.clear();
-                    return;
-                }
-                probe_limit = probe_limit.saturating_add(probe_interval).min(block_end);
             }

             if ip3 >= ilimit {
--- a/vendor/zrip-encode/src/fast.rs
+++ b/vendor/zrip-encode/src/fast.rs
@@ -394,10 +394,6 @@
         1usize << params.window_log
     };

-    let probe_interval = (block_size / 4).max(4096).min(block_size);
-    let mut probe_limit = block_start + probe_interval;
-    let mut total_match_bytes: usize = 0;
-
     let mut rep1 = rep_offsets[0] as usize;
     let mut rep2 = rep_offsets[1] as usize;
     let mut anchor = block_start;
@@ -441,7 +437,6 @@
                     hash_store!(hash_table, h1, ip1 as u32);
                     let back = ip2 - ip0;
                     let mlen = count_match!(src, ip2 + 4, ip2 - rep1 + 4, block_end) + 4 + back;
-                    total_match_bytes += mlen;
                     let lit_len = (ip0 - anchor) as u32;
                     sequences.push(Sequence {
                         literal_length: lit_len,
@@ -490,7 +485,6 @@
                 let mlen = count_match!(src, ip0 + confirm, match_idx + confirm, block_end)
                     + confirm
                     + back;
-                total_match_bytes += mlen;
                 let lit_len = (match_start - anchor) as u32;
                 sequences.push(Sequence {
                     literal_length: lit_len,
@@ -556,7 +550,6 @@
                 let mlen = count_match!(src, ip0 + confirm, match_idx + confirm, block_end)
                     + confirm
                     + back;
-                total_match_bytes += mlen;
                 let lit_len = (match_start - anchor) as u32;
                 sequences.push(Sequence {
                     literal_length: lit_len,
@@ -598,15 +591,6 @@

             #[cfg(any(target_arch = "x86_64", target_arch = "aarch64"))]
             primitives::prefetch_ht(hash_table, h_next);
-
-            if ip0 >= probe_limit {
-                let scanned = ip0 - block_start;
-                if total_match_bytes * 6 < scanned {
-                    sequences.clear();
-                    return;
-                }
-                probe_limit = probe_limit.saturating_add(probe_interval).min(block_end);
-            }

             if unlikely(ip2 + 1 >= ilimit) {
                 break;
--- a/vendor/zrip-encode/src/lib.rs
+++ b/vendor/zrip-encode/src/lib.rs
@@ -158,24 +158,10 @@
     ((window_log - 10) as u8) << 3
 }

-pub(crate) fn block_looks_incompressible(data: &[u8]) -> bool {
-    const SAMPLE: usize = 1024;
-    const DISTINCT_THRESHOLD: u32 = 200;
-    const MAX_FREQ_DENOM: u32 = 24;
-    if data.len() < SAMPLE {
-        return false;
-    }
-    let mut counts = [0u16; 256];
-    for &b in &data[..SAMPLE] {
-        counts[b as usize] += 1;
-    }
-    let mut distinct: u32 = 0;
-    let mut max_freq: u16 = 0;
-    for &c in &counts {
-        distinct += (c > 0) as u32;
-        max_freq = max_freq.max(c);
-    }
-    distinct >= DISTINCT_THRESHOLD && (max_freq as u32) <= SAMPLE as u32 / MAX_FREQ_DENOM
+// Raster byte planes may be periodic with arbitrarily distributed bytes.
+// The match finder must inspect them rather than infer matches from entropy.
+pub(crate) fn block_looks_incompressible(_data: &[u8]) -> bool {
+    false
 }

 pub(crate) fn clamp_params_to_src_size(params: &mut strategy::LevelParams, src_len: usize) {
